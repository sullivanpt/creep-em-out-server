'use strict'

const { Article } = require('./documents/article.model')
const { Member } = require('./documents/member.model')
const { Topic } = require('./documents/topic.model')

module.exports = {
  Article,
  Member,
  Topic,
}
